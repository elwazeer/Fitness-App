<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohmaedibrahim.calculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/splash"
        android:label="MotivPool"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".home"
            android:label="MotivPool"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".select"
            android:label="@string/title_activity_select" />
        <activity
            android:name=".counter"
            android:label="@string/title_activity_counter" />
        <activity
            android:name=".calories"
            android:label="@string/title_activity_calories"></activity>
    </application>

</manifest>